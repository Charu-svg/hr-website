{
  "name": "wrenfield-site",
  "version": "1.0.0",
  "private": true,
  "description": "Wrenfield recruitment site: static front end plus the API that takes briefs, applications and newsletter signups.",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "busboy": "^1.6.0"
  },
  "optionalDependencies": {
    "nodemailer": "^6.9.14"
  }
}
